# LabelBricks does NOT require a .env file.
#
# Local development auth: set your Databricks CLI profile in the shell
#   DATABRICKS_CONFIG_PROFILE=e2-demo-field-eng uv run python app.py
#
# Or fix your DEFAULT profile:
#   databricks auth login --host https://your-workspace.cloud.databricks.com
#
# User identity is auto-detected from the SDK (w.current_user.me()).
# Volume path is selected from the UI.
